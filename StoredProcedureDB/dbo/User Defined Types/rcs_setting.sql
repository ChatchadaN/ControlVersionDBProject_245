CREATE TYPE [dbo].[rcs_setting] AS TABLE (
    [Type]      VARCHAR (20) NOT NULL,
    [TRNo]      VARCHAR (20) NOT NULL,
    [Spec]      VARCHAR (20) NOT NULL,
    [PackStyle] VARCHAR (20) NOT NULL,
    [HFERank]   VARCHAR (20) NOT NULL,
    [Marking]   VARCHAR (20) NOT NULL);

